| return colorScheme |return _ WriteStream on: (String new: 300).colorScheme _ book modulesAt: 'colorScheme'."Page Buttons"return	nextPutAll: (colorScheme button: 'end')."Owner Specific"(request settingsAt: 'isOwner') ifTrue: [return	nextPutAll: (colorScheme button: 'looksbw');	nextPutAll: (colorScheme button: 'spacer')]."Logged-in Specific"request security ifNotNil: [return	nextPutAll: (colorScheme button: 'viewbw');	nextPutAll: (colorScheme button: 'editbw');	nextPutAll: (colorScheme button: 'uploadbw');	nextPutAll: (colorScheme button: 'historybw')].return	nextPutAll: (colorScheme button: 'printbw');	nextPutAll: (colorScheme button: 'spacer')."Book Buttons"return	nextPutAll: (colorScheme button: 'home' link: (request referenceShelf: shelf book: book)).return nextPutAll: ((request address = 'recent')	ifTrue: [colorScheme button: 'recentie']	ifFalse: [colorScheme button: 'recent' link: (request referenceShelf: shelf book: book address: 'recent')]).return nextPutAll: ((request address = 'search')	ifTrue: [colorScheme button: 'searchie']	ifFalse: [colorScheme button: 'search' link: (request referenceShelf: shelf book: book address: 'search')]).return nextPutAll: ((request address = 'account')	ifTrue: [colorScheme button: (request security ifNil: ['signinie'] ifNotNil: ['accountie'])]	ifFalse: [colorScheme button: (request security ifNil: ['signin'] ifNotNil: ['account']) link: (request referenceShelf: shelf book: book address: 'account')]).return nextPutAll: (colorScheme button: 'end')."Return contents"return contents