"Return the pages that were changed after a point"| limitSeconds |limitSeconds _ (Time now asSeconds) + (Date today asSeconds) - (request fieldsAsNumberKey: 'seconds' ifAbsent: [86400 "One day"]).(book pages select: [:page | (page date asSeconds + page time asSeconds) > limitSeconds])	asSortedCollection: [:a :b | (a date = b date)		ifTrue: [a time > b time]		ifFalse: [a date > b date]]